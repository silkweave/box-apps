import { defineCoreFeature } from '../../feature.js'

/** The sink: an inbox of markdown notes on disk, worked up into tracked state by hand or by agent. */
export default defineCoreFeature({
  id: 'sink',
  models: [],
  migrations: [],
})
